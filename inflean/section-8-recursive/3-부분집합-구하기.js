function solution(n) {
  let answer = [];
  let ch = Array.from({ length: n + 1 }, () => 0);

  const DFS = (v) => {};
  DFS(1);

  return answer;
}

solution(3).forEach((n) => console.log(n));
// [ '1 2 3', '1 2', '1 3', '1', '2 3', '2', '3' ]
